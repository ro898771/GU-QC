[project]
name = "gucheck"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
    "plotly>=5.0.0",
]


[tool.uv]
index-url = "https://usw2p-edge.packages.broadcom.net/artifactory/api/pypi/platform-pypi/simple"
allow-insecure-host = ["usw2p-edge.packages.broadcom.net"]
